### Under Bulid

Programs where path traversal commonly pays well
Based on public disclosures, file-handling features to prioritize manually:

File download endpoints (/download?file=, /export?report=)
Image/document viewers (/view?doc=, /preview?path=)
Template/theme loaders (/render?template=, /theme?name=)
Import/upload functionality (zip slip via multipart)
API endpoints serving static files (/api/files/{name})
